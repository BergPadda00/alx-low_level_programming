#include "main.h"
#include <stdio.h>

/**
 * _strlen - Returns the length of a string
 * @s:char
 *
 *Return:int
 */

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
continue;
}
return (i);
}

